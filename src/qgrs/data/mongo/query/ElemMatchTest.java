package qgrs.data.mongo.query;

import junit.framework.Assert;

import org.junit.Test;

public class ElemMatchTest {

	
	/*
	@Test 
	void test() {
		{ array: 
			{ $elemMatch: 
				{ value1: 1, value2: 3 } 
			} 
		}
		ElemMatch em = new ElemMatch("array");
		em.matches.add(new Match("value1", "1"));
		em.matches.add(new Match("value2", "2"));
		
		Assert.assertTrue(em.toString().equals("{array:{$elemMatch:{value1:1,value2:3} 
			} 
		}"));
	}
	*/
}
